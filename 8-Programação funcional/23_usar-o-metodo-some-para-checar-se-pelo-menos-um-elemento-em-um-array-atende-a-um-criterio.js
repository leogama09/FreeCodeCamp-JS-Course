function checkPositive(arr) {
  // Altere apenas o código abaixo desta linha
  return arr.some(elemento => elemento > 0)

  // Altere apenas o código acima desta linha
}

checkPositive([1, 2, 3, -4, 5]);